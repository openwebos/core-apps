facades directory.
A collection of static methods for managing mojodb types.

These just wrap and perform operations on db records without changing their state.

These should all be scoped under EmailApp.Facades. 

Will refactor to handle this later.